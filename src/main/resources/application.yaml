spring:
  application:
    name: lab2
  datasource:
    url: jdbc:postgresql://localhost:5432/trspz
    username: ${USERNAME}
    password: ${PASSWORD}
    driver-class-name: org.postgresql.Driver

  flyway:
    baseline-on-migrate: true
    baseline-version: 0
    enabled: true

  jpa:
    hibernate:
      ddl-auto: validate