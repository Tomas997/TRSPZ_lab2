services:
  app:
    restart: always
    build: .
    container_name: trspz_lab3
    ports:
      - "8080:8080"
  db:
    image: postgres:15.10
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: trspz
      POSTGRES_USER: postgres
    ports:
      - "5432:5432"